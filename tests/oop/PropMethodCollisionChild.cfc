/**
 * Subclass of PropMethodCollision — verifies the property/method-collision
 * resolution survives inheritance (the parent pseudo-constructor + reset() run
 * during the child's instantiation).
 */
component extends="PropMethodCollision" {
}
