[workspace]

[package]
name = "rustimagemax"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Demand-driven, tile-based image processing library written in Rust"

[dependencies]
num-traits = "0.2"
bytemuck = "1.5"

[dev-dependencies]
criterion = "0.5"
